Frontend
========

The frontend provides the Tkinter user interface and connects the view and controller.

Responsibilities
----------------

- `frontend.view`: widgets, dialogs, table rendering, form state, event handling
- `frontend.controller`: event handling, CRUD calls, filter/search calls
- `frontend.config`: shared paths for DB
- `frontend.app`: application entry point
